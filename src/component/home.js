import React from 'react'; 
let st={
    width: '100%', 
    height: 50, 
    backgroundColor: '#ff0e09', 
    color: 'white',
    paddingLeft: 100, 
    paddingTop: 10,
    fontSize: 24,
    fontWeight: 'bold',
    position: 'fixed',
    zIndex: 1
}
function Home (){ 
	return <div style={st}>Home is where the heart is...</div>
} 

export default Home; 
